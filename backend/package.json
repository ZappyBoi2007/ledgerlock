{
  "name": "ledgerlock-backend",
  "version": "0.1.0",
  "description": "LedgerLock backend – Node.js API server for document notarisation on the blockchain.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "node --test tests/",
    "hardhat:compile": "hardhat compile",
    "hardhat:test": "hardhat test",
    "hardhat:deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "keywords": ["ledgerlock", "blockchain", "ipfs", "notarisation"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.19",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
